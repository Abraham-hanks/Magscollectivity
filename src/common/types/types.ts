// import { UserAuth } from "../interface/user-auth.interface"
import { Request, Express } from 'express'
// import { Request } from "@nestjs/common"

// extend type
type UserEvent = Event & { UserId: string }

// export type Req = Request & { user: UserAuth }
